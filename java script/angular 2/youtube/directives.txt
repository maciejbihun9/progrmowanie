 * add a html section if condition is true:
 - *ngIf="showDetail === true"
 - show this section if attr value === true:
 <div *ngIf="showDetail === true">
  Phone number : {{contact.phone}}
  E-Mail : {{contact.email}}
 </div>

 * create two way data binding:
 - [(ngModel)]="contact.firstName"
 - <input [(ngModel)]="contact.firstName" type="text" />
 
 * create a one way binding from user to the element:
 - 

 * show an html component if some field is not null:
 - <app-contact-component *ngIf="selectedContact !== null"
 [contact]="selectedContact"></app-contact-component>