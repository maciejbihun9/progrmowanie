 * import ROUTER_PROVIDERS
 
 * create a new route:
 - <a [routerLink]="['NewContact']">New Contact</a>

 * we can pass an item into a path:
 - // navigate into a specified url with parameters of an url string.
    // this mthod always gets a list with, url and dict of parameters
 -  constructor (private _router: Router) {}
    this._router.navigate(['About', {contactid: _contact.id}]);