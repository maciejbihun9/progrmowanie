
 zastosowanie:
 * check boolean field and update html element depends on value:
 - <input type="checkbox" (click)="check()" [checked]="inputRegion.checked"/>

 * dodanie klas css w zalezności od tych pól:
 - <div class="control__indicator"
           [ngClass]="{'glowBorder': inputRegion.subRegionsChecked && !inputRegion.checked}"></div>