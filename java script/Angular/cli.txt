 * it causes many problems during a setup, during installation

 * build the application and prepare it for the deployment
 - ng build

 * install specific version of angular cli:
 - npm install @angular/cli@1.6.5

 * sometimes it is hard to remove angular cli version, but it can be removed from node_modules safly