
 * shareReplay - This operator returns an Observable that shares a single subscription to the underlying source, which is the Observable.
 - we have one source that notifies subscribers about new data item and emits them to subscribers once they are available,